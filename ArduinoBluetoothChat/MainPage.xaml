<Page
    x:Class="ArduinoBluetoothChat.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArduinoBluetoothChat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="status" Grid.ColumnSpan="2" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top" Margin="50"/>

        <Border Margin="100" Background="#DDFFDD">
            <StackPanel>            
                <TextBlock>Send</TextBlock>
                <TextBox Margin="5" x:Name="sendText"/>

                <TextBlock Margin="5" x:Name="sentText"/>
            </StackPanel>
        </Border>

        <Border Background="#DDDDFF" Grid.Column="1" Margin="100">
            <StackPanel>
                <TextBlock Text="Received"/>
                <TextBlock x:Name="receiveText" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
